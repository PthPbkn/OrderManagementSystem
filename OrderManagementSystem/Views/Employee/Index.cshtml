@model IEnumerable<OrderManagementSystem.Entity.ViewModels.EmployeeViewModel>
  
@{
    ViewData["Title"] = "Index";
}

@section styles
{
    <link href="~/datatables/css/jquery.datatables.min.css" rel="stylesheet" />
}

<h3>Employee List</h3>

<p>
    
</p>

<div class="card shadow p-5">
    <div class="d-grid d-md-flex justify-content-md-end pb-4">
        <a asp-action="Create" class="btn btn-primary">Add New Employee</a>
    </div>
    <table class="table" id="EmployeeTable">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(models => models.EmployeeID)
                </th>           
                <th>
                    @Html.DisplayNameFor(model => model.Title)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.FirstName)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.LastName)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Gender)
                </th>
            
                <th>
                    @Html.DisplayNameFor(model => model.HireDate)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.DepartName)
                </th>

                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model) {
                    <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.EmployeeID)
                            </td>
            
                            <td>
                                @Html.DisplayFor(modelItem => item.Title)
                            </td>
            
                        <td>
                            @Html.DisplayFor(modelItem => item.FirstName)
                        </td> 
                            <td>
                                @Html.DisplayFor(modelItem => item.LastName)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Gender)
                            </td>
                
                        <td>
                            @Html.DisplayFor(modelItem => item.HireDate)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.DepartName)
                        </td>
                        <td>
                        <a asp-action="Details" asp-route-id="@item.EmployeeID" class="btn btn-primary"><i class="fa-brands fa-readme"></i></a>
                        </td>
                    </tr>
            }
                </tbody> 
    </table>
</div>


@section scripts
{
    <script src="~/datatables/js/datatables.datatables.min.js"></script>
    <script src="~/datatables/js/jquery.datatables.min.js"></script>
    <script>
        $(document).ready(function () {
            $('#EmployeeTable').DataTable();
        });
    </script>

}
